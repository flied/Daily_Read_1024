name: Handle New Issue

on:
  issues:
    types: [opened] # 当有新 Issue 创建时触发
  workflow_dispatch: # 允许手动点按钮触发

permissions:
  contents: write
  issues: read

jobs:
  sync:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout code
        uses: actions/checkout@v3

      # 1. 发送通知到 Telegram
            # 1. 解析内容并发送通知到 Telegram
            # 1. 解析内容并发送通知到 Telegram
      - name: Send Custom Telegram Notification
        uses: actions/github-script@v6
        env:
          TG_BOT_TOKEN: ${{ secrets.TELEGRAM_BOT_TOKEN }}
          TG_CHAT_ID: ${{ secrets.TELEGRAM_CHAT_ID }}
        with:
          script: |
            const issue = context.payload.issue;
            const body = issue.body || "";
            
            // --- 1. 提取原始 URL ---
            const urlMatch = body.match(/URL:\s*(https?:\/\/[^\s\n]+)/);
            const originalUrl = (urlMatch && urlMatch[1]) ? urlMatch[1] : issue.html_url; 
            
            // --- 2. 提取评论 ---
            const commentMatch = body.match(/Comment:\s*([\s\S]*)/);
            let comment = (commentMatch && commentMatch[1]) ? commentMatch[1].trim() : "";
            
            // --- 3. 处理标签 (解决第二个标签无法点击的问题) ---
            // Github 允许 Label 带空格(如 "Read Later")，但 Telegram Hashtag 不支持空格
            // 策略：将 Label 内部的空格替换为下划线，并在 Label 之间加空格
            const tags = issue.labels.map(l => {
                // 移除可能的非法字符，将空格变下划线
                const safeName = l.name.trim().replace(/\s+/g, '_'); 
                return `#${safeName}`;
            }).join(' '); // 这里确保每个标签之间有空格

            // --- 4. 构建消息 ---
            // 使用数组过滤空内容，然后用单换行符连接，去除中间空行
            const messageParts = [];
            
            if (comment) messageParts.push(comment); // 有评论才放
            if (tags) messageParts.push(tags);       // 有标签才放
            messageParts.push(originalUrl);          // URL 必放

            const message = messageParts.join('\n'); // 使用单换行符连接

            // --- 5. 发送 ---
            const token = process.env.TG_BOT_TOKEN;
            const chatId = process.env.TG_CHAT_ID;
            
            if (!token || !chatId) return;

            try {
                await fetch(`https://api.telegram.org/bot${token}/sendMessage`, {
                    method: 'POST',
                    headers: { 'Content-Type': 'application/json' },
                    body: JSON.stringify({
                        chat_id: chatId,
                        text: message,
                        disable_web_page_preview: false 
                    })
                });
            } catch (error) {
                console.error("Failed to send Telegram notification:", error);
            }

      # 2. 自动生成 README (表格格式)

            # 2. 自动生成 README (表格格式)
      - name: Update README
        uses: actions/github-script@v6
        with:
          script: |
            const fs = require('fs');
            
            // 获取所有 open 的 issue，按创建时间倒序
            const issues = await github.paginate(github.rest.issues.listForRepo, {
              owner: context.repo.owner,
              repo: context.repo.repo,
              state: 'open',
              sort: 'created',
              direction: 'desc',
              per_page: 100
            });

            // 生成 Markdown 头部
            let content = "# 📚 My Reading List\n\n自动收集自浏览器，每日更新。\n\n";
            
            // 表格头：日期 | 标题 | 评论 | 标签
            content += "| 日期 | 标题 | 评论 | 标签 |\n";
            content += "| :--- | :--- | :--- | :--- |\n";

            issues.forEach(issue => {
              const date = new Date(issue.created_at).toISOString().split('T')[0];
              const body = issue.body || "";

              // --- 1. 解析原始 URL ---
              // 优先从 Body 中提取 URL: ...，如果没有则降级使用 Issue 链接
              const urlMatch = body.match(/URL:\s*(https?:\/\/[^\s\n]+)/);
              const url = (urlMatch && urlMatch[1]) ? urlMatch[1] : issue.html_url;

              // --- 2. 解析标题 ---
              // 移除标题中可能破坏 Markdown 表格的竖线 |
              let titleText = issue.title.replace(/\|/g, '-').trim();
              // 构建 [标题](URL) 格式
              const titleLink = `[${titleText}](${url})`;

              // --- 3. 解析评论 ---
              const commentMatch = body.match(/Comment:\s*([\s\S]*)/);
              let comment = (commentMatch && commentMatch[1]) ? commentMatch[1].trim() : "";
              // 清理评论中的换行符和竖线，防止破坏表格结构
              comment = comment.replace(/[\r\n]+/g, ' ').replace(/\|/g, '-');

              // --- 4. 解析标签 ---
              // 格式化为 #Tag1 #Tag2
              const tags = issue.labels.map(l => `\`#${l.name}\``).join(' ');

              // --- 5. 拼接表格行 ---
              content += `| ${date} | ${titleLink} | ${comment} | ${tags} |\n`;
            });

            fs.writeFileSync('README.md', content);
      # 3. 提交 README 的更改 (这是之前可能漏掉的)
      - name: Commit & Push
        run: |
          git config --global user.name 'github-actions[bot]'
          git config --global user.email 'github-actions[bot]@users.noreply.github.com'
          git add README.md
          # 检查是否有变化，有变化则提交
          if git diff --staged --quiet; then
            echo "No changes to commit"
          else
            git commit -m "Auto-update README with latest issues"
            git push
          fi
