name: Handle New Issue

on:
  issues:
    types: [opened] # 当有新 Issue 创建时触发
  workflow_dispatch: # 允许手动点按钮触发

permissions:
  contents: write
  issues: read

jobs:
  sync:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout code
        uses: actions/checkout@v3

      # 1. 发送通知到 Telegram
      - name: Notify Telegram
        uses: appleboy/telegram-action@master
        with:
          to: ${{ secrets.TELEGRAM_CHAT_ID }}
          token: ${{ secrets.TELEGRAM_BOT_TOKEN }}
          format: markdown
          message: |
            *${{ github.event.issue.title }}*
            
            ${{ github.event.issue.body }}
            
            [View on GitHub](${{ github.event.issue.html_url }})

      # 2. 自动生成 README (表格格式)
            # 2. 自动生成 README (表格格式)
      - name: Update README
        uses: actions/github-script@v6
        with:
          script: |
            const fs = require('fs');
            
            // 获取所有 open 的 issue，按创建时间倒序
            const issues = await github.paginate(github.rest.issues.listForRepo, {
              owner: context.repo.owner,
              repo: context.repo.repo,
              state: 'open',
              sort: 'created',
              direction: 'desc',
              per_page: 100
            });

            // 生成 Markdown 头部
            let content = "# 📚 My Reading List\n\n自动收集自浏览器，每日更新。\n\n";
            content += "| 日期 | 标题 | 链接 | 评论 | 标签 |\n";
            content += "| :--- | :--- | :--- | :--- | :--- |\n";

            issues.forEach(issue => {
              const date = new Date(issue.created_at).toISOString().split('T')[0];
              
              // 1. 处理标题 (移除可能存在的 Markdown 链接语法，防止破坏表格)
              let title = issue.title.replace(/\|/g, '-').trim();

              // 2. 处理标签
              const tags = issue.labels.map(l => `\`${l.name}\``).join(' ');

              // 3. 解析 Body (关键修改部分)
              let url = issue.html_url; // 默认为 Issue 地址作为保底
              let comment = "";
              const body = issue.body || "";

              // 尝试解析书签发送的标准格式
              // 格式: Title: ... \n URL: ... \n\n Comment: ...
              const urlMatch = body.match(/URL:\s*(https?:\/\/[^\s\n]+)/);
              const commentMatch = body.match(/Comment:\s*([\s\S]*)/); // 匹配 Comment: 后面的所有内容

              if (urlMatch && urlMatch[1]) {
                  url = urlMatch[1];
              }
              
              if (commentMatch && commentMatch[1]) {
                  comment = commentMatch[1].trim();
              } else if (!urlMatch) {
                  // 如果不是标准格式（比如手动提的 Issue），就把整个 body 当评论
                  comment = body;
              }

              // 4. 清理数据以适配 Markdown 表格
              // 将 URL 转换为 Markdown 链接节省空间
              const urlLink = `[Link](${url})`; 
              // 清理评论中的换行符和管道符
              comment = comment.replace(/[\r\n]+/g, ' ').replace(/\|/g, '-').trim();
              
              content += `| ${date} | ${title} | ${urlLink} | ${comment} | ${tags} |\n`;
            });

            fs.writeFileSync('README.md', content);

      # 3. 提交 README 的更改 (这是之前可能漏掉的)
      - name: Commit & Push
        run: |
          git config --global user.name 'github-actions[bot]'
          git config --global user.email 'github-actions[bot]@users.noreply.github.com'
          git add README.md
          # 检查是否有变化，有变化则提交
          if git diff --staged --quiet; then
            echo "No changes to commit"
          else
            git commit -m "Auto-update README with latest issues"
            git push
          fi
